@model FoodListViewModel
@{
}
<div class="text-center">
    <h2>My Order</h2>
    <div class="col-sm-10">
        <table class="table table-bordered table-striped mt-2">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Price</th>
                </tr>
            </thead>
            <tbody>
                @foreach (Food food in Model.Foods)
                {
                    int count = 0;
                    <tr>
                        <td>@food.Name</td>
                        <td>@food.Price.ToString("c")</td>
                        <td>
                            <form asp-controller="Home" asp-action="Remove">
                                <button type="submit" name="count" value="@count">Remove</button>
                            </form>
                        </td>
                        
                    </tr>
                    ViewBag.Total += food.Price;
                    count++;
                }
                <tr>
                    <td>Total</td>
                    <td>@ViewBag.Total.ToString("c")</td>
                </tr>
            </tbody>
        </table>
       
        
        
        <form asp-action="Delete" method="post">
            <button type="submit" class="btn btn-primary">
                Clear Order
            </button>
        </form>
    </div>

</div>




